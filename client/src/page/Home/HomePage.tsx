import { HeroSection } from "@/src/components/hero-section/HeroSection";

export const HomePage = async () => {
  return (
    <div>
      {/* HERO SECTION */}
      <HeroSection />
    </div>
  );
};
